You produce a T-shirt catalog from supplied synthetic evidence. Return one JSON object with case_id and records. No web, no external facts, no code execution to generate answers, no grader access. Use only the supplied input and these rules.

Output records: {sku, role, parent_sku, fields, evidence, status, issues, display, measurements}. fields retains all supplied applicable facts and percentages, with only supplied alias normalization. evidence maps every output field to the source evidence ID. For explicitly listed inherit_fields, copy the parent's field and cite the parent field evidence. Otherwise do not enrich/change parent or siblings. Omit genuinely missing fields; do not invent values.

Parent is a supplier-designated flagship with its own color/size/price. Preserve links. Required: brand, category, subcategory, design, pattern, material, color, size, price and subbrand if profile.subbrand_applicable. Missing facts block only affected SKUs. Unmapped allowed-list values block. Family invariants: brand, subbrand if applicable, category, subcategory, design, pattern, material. A child differing from parent blocks while an otherwise correct parent remains READY. Generic-versus-specific material is a mismatch unless supplier resolution was already supplied. Explicit parent inheritance is allowed only via fixture inherit_fields.

status READY if no blockers; otherwise BLOCKED. issues contains only actual blockers, each {code,field,evidence:[source IDs],action:nonempty corrective instruction}. Codes: MISSING_REQUIRED (empty evidence list); UNMAPPED_VALUE (that field's source); FAMILY_MISMATCH (child and parent field sources); COMPOSITION_EXCESS (own material source); NAMED_BLEND_MAJORITY (own material source). Do not report nonblocking warnings as issues. Correctly BLOCKED work can be correct handling.

Material sum >101 blocks; 100..101 or shortfall is nonblocking. Preserve all percentages internally. Named blend with known percentages requires named fibre >50%. Generic labels without percentages allowed; no minimum sum. display is {text, derived_remainder?}. Exactly two numeric components with larger >50 and smaller <10: text shows larger only. Other two: both, no others. Three+ all-percent components short of 100: show all plus calculated N% others and derived_remainder as a decimal string. Otherwise show supplied components or generic label. Descending preferred but ordering alone not a failure. Text format: percentages followed by material, components separated by commas, e.g. "80% cotton, 20% polyester"; generic label verbatim. Preserve recycled/other unrelated metadata if supplied, never double-count it.

measurements: one per input measurement, each {id, source_sku, source_ref, measurement_type, source_value, source_unit, value, unit}. Keep source identity/value/unit exact and convert in to cm with 2.54, decimal half-up to 1 decimal. value/source_value can be decimal strings; unit='cm'. Preserve supplied sizes, do not infer them. Do not copy sibling measurements. Emit [] when none. A correct conversion is preferred; <=0.5cm deviation only warns except crossing a supplied size-band boundary always fails. Any structured extra factual claim needs source evidence.

Do the task faithfully; do not attempt to exploit the checker. Return the raw JSON object without markdown. No gold judgments or grader code are provided.
