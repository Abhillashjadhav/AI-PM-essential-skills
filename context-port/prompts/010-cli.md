# Phase 10 prompt: public CLI

## Date

2026-07-11

## Branch

`context-port/010-cli`

## Public task record

Consolidate the standalone ContextPort command-line interface into a documented public contract without changing prior engine behavior. Add stable version discovery, deterministic machine-readable capability discovery, explicit exit-code documentation, help coverage, tests, and evaluation evidence. Keep stdout pipe-friendly, errors on stderr, production dependencies at zero, and all destination writes unsupported.

## Status

Complete. The CLI exposes stable version and capability discovery while preserving prior command behavior and documenting all output channels and control-flow statuses.
