# Review of PR #37: Add agent graph designer marketplace plugin

**Date:** 2026-08-19
**Final state:** APPROVE after one repair round

## Round 1 findings (reviewer role)

### Major — failure edges did not satisfy the blocked terminal contract

The `blocked` terminal requires `blocking_reason`, but exhausted-review, join-failure, and human-rejection edges had empty `state_mapping` objects. The topology could reach `BLOCKED` without carrying the evidence needed to explain why or how to unblock.

### Major — declared exhaustion and timeout behavior was not fully represented by edges

`freeze-candidate` declared `on_exhaustion: BLOCKED` but had no failure edge. Specialist retries covered verification failure but not timeout, even though each node declared a timeout. These gaps contradicted the skill's no-silent-success rule.

### Minor — skill frontmatter missed the repository quality-bar field

`CLAUDE.md` requires `argument-hint` in skill frontmatter. The new skill supplied name and description only.

## Round 1 actions (writer role)

- Added blocking-evidence mappings to every edge entering `blocked`.
- Added a freeze-candidate failure/timeout edge.
- Made specialist retry and exhaustion conditions explicitly cover timeout.
- Added `argument-hint` to the skill frontmatter.
- Strengthened the contract validator to reject undeclared evidence mappings, unknown target inputs, terminal edges with missing mappings, and `BLOCKED` exhaustion without a terminal edge.
- Updated the sample topology and verification statement.

## Reviewer re-check

- Architecture: PASS — graph qualification stays separate from loop execution; `loop-designer` is unchanged.
- Security and permissions: PASS — sample is synthetic, local-only, least-privilege, and performs no consequential action.
- Business logic: PASS — every declared failure or timeout now has visible routing and blocking evidence.
- Scalability: PASS — node, concurrency, retry, time, and cost caps remain explicit.
- Simplicity: PASS — one additive plugin, one known-answer graph, one standard-library runner, and no production dependency.

## Final verdict

`APPROVE`

The automated checks listed in the PR must pass again on the repaired head. The repository owner retains the merge decision.

## README prompt follow-up

### Findings

- Major: the first loop example omitted its timezone, state path, and output destination, so it would require a follow-up interview before scheduling.
- Major: the first graph example described topology without the user's problem, hypothesis, outcome North Star, metrics, guardrails, trade-off, or proposed next step.

### Actions

- Added a checkable success condition, timezone, source list, seen-log path, output path, and complete run reporting to the loop prompt.
- Added the product outcome contract ahead of the graph topology request.

### Re-check

`APPROVE` — the examples now demonstrate both the orchestration shape and the minimum product judgment each skill must preserve.

## Case-study correction

### Finding

- Major: the README used two unrelated prompt templates. It did not demonstrate how one real product problem moves from a bounded loop to a coordinated graph.

### Action

- Replaced both templates with one AI support-ticket summarizer case study.
- Scoped the loop to a recurring nightly cycle that repairs and verifies one frozen candidate without deploying it.
- Scoped the graph to coordinating product, model-quality, and safety/privacy evidence for the launch decision.
- Added a shared decision boundary: improve one bounded unit with a loop; coordinate multiple bounded units with a graph.

### Re-check

`APPROVE` — the README now shows the relationship between loop engineering and graph engineering through one continuous case.
