# Phase 8 prompt: incremental sync engine

## Date

2026-07-11

## Branch

`context-port/008-incremental-sync`

## Public task record

Build a deterministic assistant-neutral incremental sync planner over validated ContextPacks. Detect additions, edits, renames, moves, reorderings, and deletions. Emit tombstones rather than deleting content. Detect divergent peer changes without semantic conflict resolution. Include checkpoints, stable digests, replay identity, tests, schema, documentation, and evaluation. Perform no writes.

## Status

In progress.
